name: budget_familial
description: Application budget familial
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  excel: ^4.0.6
  path_provider: ^2.1.5
  share_plus: ^12.0.1
  purchases_flutter: ^8.11.0
  google_mobile_ads: ^7.0.0
  app_tracking_transparency: ^2.0.6+1
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  firebase_core_platform_interface: ^5.4.2

flutter:
  uses-material-design: true
  generate: true